/** Group / message limits. */

export const GROUP_MAX_MEMBERS = 100;

/** How long (ms) a sender has to edit their own message. */
export const MESSAGE_EDIT_WINDOW_MS = 15 * 60 * 1000; // 15 minutes
